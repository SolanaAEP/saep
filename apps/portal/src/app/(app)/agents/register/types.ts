export type WizardData = {
  seed: string;
  manifestUri: string;
  selectedBits: Set<number>;
  priceSol: string;
  streamRate: string;
  stakeAmount: string;
  stakeMint: string;
  operatorAta: string;
};
