# Final Beacon Record

- Announced at: `<UTC_TIMESTAMP>`
- Beacon chain: `bitcoin`
- Block height: `<BLOCK_HEIGHT>`
- Block hash: `<BLOCK_HASH_HEX>`
- Applied by: `<NAME / HANDLE>`

Command used:

```bash
snarkjs zkey beacon \
  task_completion_<LAST>.zkey \
  task_completion_final.zkey \
  <BLOCK_HASH_HEX> \
  10 \
  -n="Final Beacon"
```

Post-beacon artifact checks:

- Final zkey SHA-256: `<SHA256>`
- Final zkey host URL: `<URL>`
- Verification run by:
  - `<NAME / HANDLE> - <DATE>`
  - `<NAME / HANDLE> - <DATE>`
  - `<NAME / HANDLE> - <DATE>`
