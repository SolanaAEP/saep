# Contribution <NNNN> - <HANDLE>

## Identity

- Name / handle: `<VALUE>`
- Affiliation: `<VALUE>`
- Contact / public profile: `<URL>`

## Artifact

- Input zkey: `task_completion_<PREV>.zkey`
  - URL: `<URL>`
  - SHA-256: `<SHA256>`
- Output zkey: `task_completion_<NNNN>.zkey`
  - URL: `<URL>`
  - SHA-256: `<SHA256>`
- Contribution hash (`snarkjs zkey contribute -v`): `<HASH>`

## Environment

- Hardware: `<VALUE>`
- OS: `<VALUE>`
- Entropy sources: `<VALUE>`

## Destruction

- Power-cycled at: `<UTC_TIMESTAMP>`
- Storage wipe method: `<VALUE>`
- Video URL: `<URL>`
- Video hash: `<HASH>`

## Attestation

I, `<NAME / HANDLE>`, attest that:

1. I executed this contribution on an isolated machine prepared for the ceremony.
2. I destroyed all randomness used in the contribution.
3. I have no undisclosed affiliation that would invalidate my contribution.

Signed:

`<SIGNATURE OR REFERENCE>`
