# Wallets

This directory is gitignored. Local keypairs only — never commit.

- `deployer.json` — localnet deployer (created by bootstrap.sh if absent)
- `test-*.json` — ephemeral test identities

Production deploy keys live in Squads multisig, not here.
